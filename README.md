# ToDo Application

A simple, responsive ToDo application built with SvelteKit and TypeScript. Manage your tasks with an intuitive user
interface.

## Prerequisites

- Node.js (Latest LTS version recommended)
- npm (comes with Node.js)

## Installation

1. Clone the repository
2. Navigate to the project directory
3. Install dependencies:
